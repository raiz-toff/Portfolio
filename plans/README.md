# Implementation Plans

Generated by the improve skill on 2026-07-12, against commit `0c23f0b`.
The session ran non-interactively, so the top findings by leverage were
selected by default (audit: standard depth, all nine categories, four
parallel auditors, every finding re-verified against the code before
planning).

Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Make `npm run lint` exit clean (0 errors) | P1 | M | — | DONE |
| 002  | Add a Vitest baseline covering the URL/content helpers | P1 | M | 001 | TODO |
| 003  | Add a GitHub Actions CI gate | P1 | S | 001, 002 | TODO |
| 004  | Close the open image proxy + harden HTML-injection sinks | P2 | S | — | TODO |
| 005  | Fetch GitHub contributions server-side with daily revalidation | P2 | M | — | DONE |
| 006  | Repo hygiene: dead scripts, stale paths, env/agent contracts | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001**: both edit `source.config.ts` (001 types the remark
  plugins; 002 moves the pure helpers out). Running 002 first would make
  001's excerpts stale.
- **003 requires 001 + 002**: the CI workflow runs `npm run lint` and
  `npm test`; both must be green/exist before the gate goes up.
- 004, 005, 006 are independent of everything and of each other — safe to
  run in parallel by separate executors on separate branches.
- 006's AGENTS.md content references conventions introduced by 001/002;
  the plan tells the executor to describe only what has actually landed.

## Verification baseline (recorded during recon)

- `npm install --legacy-peer-deps` — required install flag (fumadocs peer skew)
- `npm run types:check` — PASSES at `0c23f0b`
- `npm run lint` — FAILS at `0c23f0b` with 19 errors (plan 001 fixes)
- `npm run build` — passes
- No tests, no test runner, no CI existed at planning time

## Findings considered and rejected / deferred

(So nobody re-audits them next run.)

- **"Broken internal doc links missing the `/docs` prefix"** — REJECTED as
  by-design: `rewriteInternalPath` in `source.config.ts:88-102` rewrites
  `/labs|/projects|/ccna-labs` links to `/docs/...` at build time, for both
  markdown links and raw `<a href>`. Only the six CCNA slug typos were
  real → folded into plan 006.
- **setState-in-effect in greeting/phone-item/share-menu as "bugs"** —
  by-design hydration-safe placeholder pattern; addressed purely as lint
  compliance in plan 001, no behavior change.
- **npm audit: moderate postcss advisory** — transitive inside `next`
  itself; not fixable from this repo. Re-check on the next Next.js bump.
- **DEP-01: `fumadocs-mdx@15` a major behind core/ui@16 (forces
  `--legacy-peer-deps`)** — DEFERRED, real migration debt, but a
  cross-major MDX-pipeline bump needs supervised execution, not a
  weak-executor plan. Revisit as its own plan after CI (003) exists.
- **DEP-02: `cnfast@0.0.8` pre-release on the styling hot path** —
  monitor, don't act: it's isolated behind the `src/lib/cn.ts` seam (a
  one-file swap to `clsx`+`tailwind-merge` if it ever misbehaves).
- **PERF-03: Devanagari webfont (2 weights) for one logotype word** —
  DEFERRED: real payload, but the fix trades against brand identity
  (inline-SVG logotype vs. webfont); owner call. Cheap middle ground if
  wanted: drop to one weight.
- **DEBT-02: Starlight-era frontmatter + ~115-line inline `<style>` blocks
  in migrated MDX** — DEFERRED: cosmetic/consistency, medium effort across
  many content files; best done alongside the CCNA content push.
- **DEBT-04: `/cover` design-playground route ships in production; hero
  eagerly imports ~2.4k lines of interactive cover code** — DEFERRED:
  route is deliberately `robots: { index: false }` and the hero is the
  site's centerpiece; lazy-splitting it risks a visible hydration flash.
  Measure bundle first before acting.
- **SEC-04: no CSP/security headers** — DEFERRED: defense-in-depth on a
  no-auth static site; a mistuned CSP breaks the migrated MDX inline
  styles and mermaid. Becomes easier after plan 004 lands.
- **`package.json` name `rn-portfolio-tmp`** — cosmetic; rename anytime.

## Direction findings (owner decisions, not executor plans)

These came out of the audit grounded in repo evidence; they are content /
product work for the owner rather than plans a code executor can run:

1. **Publish the mega-lab** (`content/docs/labs/mega-lab/` — 15 files, all
   `draft: true`, phases 1–13 substantially written, phase 14 an empty
   stub). Cheapest large content win: finish phase 14, editorial pass,
   lift the drafts.
2. **Deliver the CCNA Labs section** — the docs landing topology hardcodes
   it `status: 'admin-down'` (`src/app/(fuma-home)/docs/page.tsx:69`) and
   all six section pages are "(Add your lab files here)" stubs, while CCNA
   is the site's core credential. Plan 006 fixes its hub links so it's
   ready to publish into.
3. **The network-generator project** — a sync pipeline was written for a
   repo that doesn't exist yet ("UPDATE THIS URL once you create the new
   repo!"), and the site hand-authors interactive topology HTML the tool
   would presumably generate. Decide: build it, or retire the intent
   (plan 006 deletes the dead script either way).
